server:
  port: 8081
  context-path:
spring:
  application:
    name: ms-report
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
#      password: ${CONFIG_SERVICE_PASSWORD}
#      username: user